 # Author: PAS Group DTU

# Modality & task configuration
modalities:
  primary: rgb
  secondary: depth
task: detection

# Runtime configuration
model_path: /home/bdasz/Desktop/SAVA/models/2d_final/onnx/inference_model.onnx
runtime: onnxruntime
providers:
  - CUDAExecutionProvider
  - CPUExecutionProvider
batch_size: 1 # For now, 1 is the only accepted value

# Model configuration
input_size: [768, 384] # [height, width]
confidence_threshold: 0.7
classes:
  - Car
  - Two Wheeler
  - Pedestrian
 